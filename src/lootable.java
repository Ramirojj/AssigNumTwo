////////////////////////// LOOTABLE INTERFACE //////////////////////////
////Modify the Player class   to interact with Lootable objects.
interface Lootable {
    String loot(Player player);
}
/*. Lootable: Contains one method named loot that takes a Player object as an argument
and returns a String. The String will be displayed to the Player after they loot the Room.*/
