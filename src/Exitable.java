
/*Exitable: Contains one method named exit that takes a Player object as an argument and returns a String.
 The String will be displayed to the Player after they exit the Room.*/
public interface Exitable {/// interface for objects that can be exited

   ////method to handle exit action
    String exit(Player player);
}